with open('write_file.txt', 'w') as f:
    f.write('ini adalah file yang dibuat dengan python')

# w means write, if the file already exists, it will be overwritten
# r means read, if the file does not exist, it will raise an error
# a means append, it will add the text to the end of the file   
# r+ means read and write
# w+ means write and read
# a+ means append and read
# b means binary mode

